# Optional: a throwaway Postgres matching application.yml's defaults.   docker compose up -d
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: davkit_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "127.0.0.1:5432:5432"
