# Kept in lockstep with davkit-core and davkit-grails-plugin by hand; all three bump together at
# release. The core is depended on at this exact version.
version=0.3.0-SNAPSHOT

org.gradle.jvmargs=-Xmx1g
org.gradle.caching=true
org.gradle.parallel=true
